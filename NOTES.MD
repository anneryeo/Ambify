## Running Ambify

To run the current version with Expo:

```bash
npx expo start
```

Then press:
- `a` for Android emulator
- `i` for iOS simulator
- `w` for web (web sucks plz do not do this, didnt code for web)

To clear cache if experiencing issues:
```bash
npx expo start --clear
```